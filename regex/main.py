import re

# def validar_email(email):
#     padrao = r'^[a-zA-z0-9._%+-]+@[a-zA-Z0-9.-]+\.[a-zA-Z]{1,2}$'
#     return bool (re.match(padrao, email))

# emails = ['usuario@exemplç.com', 'nome.sobrenome@empresa.com.br', 
#          'invalido@', 'sem_arroba.com', '@dominio.com', 'guilherme.+_-p@ienh.com.br.asdf']

# for email in emails:#para cada email dentro da lista emails
#     print(f"{email}: {'Válido' if validar_email(email) else 'Inválido'}")
